#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-simple-router/bin/mk-server" "$@"
  ret=$?
else 
  node  "$basedir/../node-simple-router/bin/mk-server" "$@"
  ret=$?
fi
exit $ret
